const todos = ['make bed', 'brush teeth', 'get milk', 'walk the dog', 'go to the gym']

console.log(`You have ${todos.length} todos on your list today`)
console.log(`Todo 1: ${todos[0]}, Todo 2: ${todos[1]}.`)